# normal
normal1: 'bg blue1' # 2 items
normal2: 'bg blue2' # 2 items
normal3: 'blue1 plomo' # 1 item
# inactive
inactive1:  'grisclaro grisoscuro' # 2 items
inactive2: 'grisclaro grisoscuro' # 2 items, linking to normal.right by default
inactive3: 'grisclaro plomo' # 1 item, linking to normal.middle by default
# insert
insert1: 'bg green1' # 2 items
insert2: 'bg green2' # 2 items, linking to normal.right by default
insert3: 'green1 plomo' # 1 items, linking to normal.middle by default
# replace
replace1: 'bg red1' # 2 items
replace2: 'bg red2' # 2 items, linking to normal.right by default
replace3: 'red1 plomo' # 1 items, linking to normal.middle by default
# visual
visual1: 'bg yellow2 ' # 2 items
visual2: 'bg yellow3' # 2 items, linking to normal.right by default
visual3: 'yellow2 plomo' # 1 items, linking to normal.middle by default
